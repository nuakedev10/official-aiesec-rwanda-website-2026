# Base URL of the Express/Drizzle backend API (see ../README.md).
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Base URL this site is served from, used for absolute URLs in metadata/sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
